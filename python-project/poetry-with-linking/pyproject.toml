[tool.poetry]
name = "poetry-with-linking"
version = "0.1.0"
description = ""
authors = ["Matthew Kenigsberg <matthew@floxdev.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.12"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
