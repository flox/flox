The prior_release_baselines/ fixtures have not yet been captured
for a real prior Flox release.

The manifest.toml files in plain/ and with_include/ are hand-authored
templates. The manifest.lock files are placeholder stubs.

Unit tests that depend on the captured lockfiles are marked #[ignore].
The bats test in activate.bats skips automatically while this file exists.

To capture real fixtures:
  just regen-prior-release-fixtures

See README.md for the full procedure.
After capture, delete this file and remove #[ignore] from the unit tests.
